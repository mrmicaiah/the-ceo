# Bearer token sent on every API call. Same value as the Worker's auth token.
# v0: the Worker does not validate this yet, but the frontend sends it so
# we have the channel ready when we harden auth.
VITE_AUTH_TOKEN=dev-token-replace-me
